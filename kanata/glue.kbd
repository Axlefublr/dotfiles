(deflayermap (#←)
	f (multi (layer-while-held arrow) (layer-while-held #arrow))
	e (layer-while-held #hack)
)

(deflayermap (!←)
	g (layer-while-held !apps←)
)

(deflayermap (!→)
	h (layer-while-held !apps→)
)

(deflayermap (#arrow)
	e (layer-while-held #hack-arrow)
)
