(deflayermap (x)
	;; [[sort on]]
	, S-1
	. (cmd wtype …)
	/ bksl
	c (multi (layer-while-held c) (layer-while-held xc) (on-release tap-vkey any-c))
	h S-4
	i S-8
	j S-eql
	k min
	l eql
	m S-5
	n (cmd wtype ×)
	o S-3
	p (cmd wtype √)
	u (cmd wtype ÷)
	;; [[sort off]]
)

(deflayermap (c)
	v (multi (layer-while-held v) (layer-while-held cv) (on-release tap-vkey any-v))
	x (multi (layer-while-held x) (layer-while-held xc) (on-release tap-vkey any-x))
	;; [[sort on]]
	, S-7
	. (cmd wtype °)
	/ S-bksl
	h (cmd wtype €)
	i S-6
	j grv
	k S-min
	l (cmd wtype —)
	m S-grv
	n (cmd wtype ¢)
	o S-2
	p (cmd wtype ™)
	u (cmd wtype ₽)
	;; [[sort off]]
)

(deflayermap (xc)
	m (cmd wtype ≻)
	n (cmd wtype ≺)
	j (cmd wtype ≤)
	k (cmd wtype ≥)
	l (cmd wtype ≈)
	. (cmd wtype ⤓)
	, (cmd wtype ⤒)
	; (cmd wtype ␈)
	' (cmd wtype ␡)
	i (cmd wtype ⊼)
	o (cmd wtype ⊻)
	spc (cmd wtype ␣)
)

(deflayermap (v)
	c (multi (layer-while-held c) (layer-while-held cv) (on-release tap-vkey any-c))
	;; [[sort on]]
	' (cmd wtype ■)
	, (cmd wtype ❗)
	/ (cmd wtype ❓)
	; (cmd wtype █)
	b (cmd wtype 💡)
	h (cmd wtype ←)
	i (cmd wtype ✨)
	j (cmd wtype ↓)
	k (cmd wtype ↑)
	l (cmd wtype →)
	m (cmd wtype ✅)
	n (cmd wtype ❌)
	o (cmd wtype ↪)
	u (cmd wtype 󰟢)
	;; [[sort off]]
)

(deflayermap (cv)
	, (cmd wtype ‼️)
	/ (cmd wtype ⁉️)
)

(deflayermap (u)
	i (multi (layer-while-held i) (layer-while-held iu) (on-release tap-vkey any-i))
	;; [[sort on]]
	a (cmd wtype 😇)
	c (cmd wtype 😢)
	d (cmd wtype 😈)
	e (cmd wtype 🗣️)
	f (cmd wtype 🔥)
	g (cmd wtype 😎)
	r (cmd wtype 🚀)
	s (cmd wtype 😔)
	t (cmd wtype 🤔)
	w (cmd wtype ✍️)
	x (cmd wtype 🤯)
	z (cmd wtype 💯)
	;; [[sort off]]
)

(deflayermap (iu)
	c (cmd wtype 💗)
	e (cmd wtype 👀)
	r (cmd wtype 🙄)
	s (cmd wtype 💀)
	w (cmd wtype 😩)
)

(deflayermap (r)
	e (multi (layer-while-held e) (layer-while-held er) (on-release tap-vkey any-e))
	;; [[sort on]]
	, (cmd wtype 🕯️)
	i (cmd wtype 😌)
	k (cmd wtype 😼)
	l (cmd wtype 🤤)
	m (cmd wtype 🧐)
	n (cmd wtype 🥺)
	o (cmd wtype 😭)
	p (cmd wtype 👏)
	u (cmd wtype 😋)
	y (cmd wtype 🥳)
	;; [[sort off]]
)

(deflayermap (er)
	h (cmd wtype 🥰)
	p (cmd wtype 😤)
	n (cmd wtype 🥹)
)

(deflayermap (i)
	j (multi (layer-while-held j) (layer-while-held ij) (on-release tap-vkey any-j))
	o (multi rmet (layer-while-held o) (layer-while-held io) (on-release tap-vkey any-o))
	u (multi (layer-while-held u) (layer-while-held iu) (on-release tap-vkey any-u))
	;; [[sort on]]
	a (cmd wtype “)
	c ]
	d S-0
	e S-]
	f (cmd wtype ”)
	g (cmd wtype „)
	q (cmd wtype ‘)
	r (cmd wtype ’)
	s S-9
	v (cmd wtype »)
	w S-[
	x [
	z (cmd wtype «)
	;; [[sort off]]
)

(deflayermap (ij)
	o (multi rmet (layer-while-held o) (layer-while-held io) (layer-while-held jo) (layer-while-held ijo) (on-release tap-vkey any-o))
	f (cmd wtype ¹)
	d (cmd wtype ²)
	s (cmd wtype ³)
	r (cmd wtype ⁴)
	e (cmd wtype ⁵)
	w (cmd wtype ⁶)
	v (cmd wtype ⁷)
	c (cmd wtype ⁸)
	x (cmd wtype ⁹)
	a (cmd wtype ⁰)
	z (cmd wtype ⁻)
	q (cmd wtype ⁺)
)

(deflayermap (jo)
	i (multi (layer-while-held i) (layer-while-held io) (layer-while-held ij) (layer-while-held ijo) (on-release tap-vkey any-i))
	f (cmd wtype ₁)
	d (cmd wtype ₂)
	s (cmd wtype ₃)
	r (cmd wtype ₄)
	e (cmd wtype ₅)
	w (cmd wtype ₆)
	v (cmd wtype ₇)
	c (cmd wtype ₈)
	x (cmd wtype ₉)
	a (cmd wtype ₀)
	z (cmd wtype ₋)
	q (cmd wtype ₊)
)
