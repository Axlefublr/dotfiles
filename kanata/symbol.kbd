(deflayermap (symbol-primary)
	;; [[sort on]]
	, S-1
	. (cmd wtype …)
	/ bksl
	h S-4
	i S-8
	j S-eql
	k min
	l eql
	m S-5
	o S-3
	u (cmd wtype ¢)
	;; [[sort off]]
)

(deflayermap (symbol-secondary)
	;; [[sort on]]
	, S-7
	. (cmd wtype °)
	/ S-bksl
	h (cmd wtype €)
	i S-6
	j grv
	k S-min
	l (cmd wtype —)
	m S-grv
	n (cmd wtype √)
	o S-2
	p (cmd wtype ™)
	u (cmd wtype ÷)
	;; [[sort off]]
)

(deflayermap (symbol-tertiary)
	;; [[sort on]]
	' (cmd wtype ┗)
	, (cmd wtype ❗)
	/ (cmd wtype ❓)
	; (cmd wtype ┃)
	b (cmd wtype 💡)
	h (cmd wtype ←)
	i (cmd wtype ✨)
	j (cmd wtype ↓)
	k (cmd wtype ↑)
	l (cmd wtype →)
	n (cmd wtype ❌)
	o (cmd wtype ✅)
	u (cmd wtype ₽)
	;; [[sort off]]
)

(deflayermap (symbol-left)
	;; [[sort on]]
	a (cmd wtype 🙏)
	c (cmd wtype 😢)
	d (cmd wtype 💃)
	e (cmd wtype 🗣️)
	f (cmd wtype 🔥)
	g (cmd wtype 😎)
	r (cmd wtype 🚀)
	s (cmd wtype 😔)
	t (cmd wtype 🤔)
	w (cmd wtype ✍️)
	x (cmd wtype 🤯)
	;; [[sort off]]
)

(deflayermap (symbol-right)
	;; [[sort on]]
	, (cmd wtype 🕯️)
	i (cmd wtype 😌)
	k (cmd wtype 😼)
	l (cmd wtype 🥀)
	m (cmd wtype 🧐)
	n (cmd wtype 🥺)
	o (cmd wtype 😭)
	p (cmd wtype 👏)
	u (cmd wtype 😋)
	y (cmd wtype 🥳)
	;; [[sort off]]
)

(deflayermap (brackets)
	;; [[sort on]]
	a (cmd wtype “)
	c ]
	d S-0
	e S-]
	f (cmd wtype ”)
	q (cmd wtype ‘)
	r (cmd wtype ’)
	s S-9
	t (cmd wtype ‚)
	v (cmd wtype »)
	w S-[
	x [
	z (cmd wtype «)
	;; [[sort off]]
)
